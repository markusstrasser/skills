{
  "findings": [
    {
      "category": "bug",
      "severity": "high",
      "title": "\"Oldest marker\" selection logic is incorrectly implemented",
      "description": "The comment in `hooks/sessionend-overview-trigger.sh` states it should use the oldest per-type marker as a diff baseline, but the code actually selects the first marker found in the configured types order and breaks immediately. This is an ordering property failure.",
      "file": "hooks/sessionend-overview-trigger.sh",
      "line": 0,
      "fix": "Remove the 'break' statement and iterate through all markers to compare mtimes, selecting the one with the earliest timestamp.",
      "confidence": 1.0,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 1
    },
    {
      "category": "logic",
      "severity": "high",
      "title": "Staleness check logic is broken for multi-type projects",
      "description": "In `hooks/overview-staleness-cron.sh`, the script picks the first existing marker file it finds and computes age from only that file. If the first marker is fresh but subsequent configured types are stale, the stale types will never be regenerated.",
      "file": "hooks/overview-staleness-cron.sh",
      "line": 0,
      "fix": "Evaluate staleness for every configured type in the loop rather than exiting after the first found marker, or aggregate the staleness state across all markers.",
      "confidence": 1.0,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 2
    },
    {
      "category": "missing",
      "severity": "high",
      "title": "Golden fixture test for plan-close context was not implemented",
      "description": "The review says the migration plan explicitly required \"golden fixture tests for plan-close packets\" to catch silent drift, but no corresponding test was added. It specifically calls out that `test_build_plan_close_context.py` does not exist and that there are no added or modified tests covering `build_plan_close_context.py`. This leaves formatting, ordering, and truncation behavior unverified after the shell-to-Python migration.",
      "file": "review/scripts/test_build_plan_close_context.py",
      "line": 0,
      "fix": "Add a golden-fixture test for `build_plan_close_context.py` using a static/mock git fixture and exact string assertions against a checked-in expected packet output.",
      "confidence": 0.98,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 3
    },
    {
      "category": "logic",
      "severity": "high",
      "title": "`generate_overview.py` mutates the historical prompt by appending `trailing_text`",
      "description": "The review flags that `generate_overview.py` injects a `trailing_text` instruction \u2014 `\"Write the requested codebase overview in markdown.\"` \u2014 into the prompt. It states this changes the legacy prompt shape and breaks the required prompt/payload equivalence guarantees, with no prompt-equivalence or payload-hash regression test to document the change. The same issue is reiterated as both \"Contract Drift (Prompt Shape)\" and \"Silent Prompt Mutation.\"",
      "file": "scripts/generate_overview.py",
      "line": 0,
      "fix": "Remove the extra `trailing_text` from overview packets to preserve backward-compatible prompt shape, or add an explicit equivalence/regression test if the prompt contract is intentionally changing.",
      "confidence": 0.97,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 4
    },
    {
      "category": "bug",
      "severity": "high",
      "title": "Overview generation no longer prepends the historical provenance header",
      "description": "The review notes that the previous shell implementation prepended a metadata comment like `<!-- Generated: ${gen_ts} | git: ${git_sha} | model: ${OVERVIEW_MODEL} -->`, but the new Python path writes the LLM output verbatim via `distribute_results` / `r.get(\"content\", \"\")`. That means generated markdown permanently loses generation timestamp, git SHA, and model provenance that existed in historical outputs.",
      "file": "scripts/generate_overview.py",
      "line": 0,
      "fix": "Prepend the historical HTML comment before writing overview markdown, e.g. generate UTC timestamp, current git SHA, and profile/model name, then write the header followed by the model output.",
      "confidence": 0.95,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 5
    },
    {
      "category": "architecture",
      "severity": "medium",
      "title": "Overview batch orchestration is brittle because it shells out and scrapes stdout for the job ID",
      "description": "The review calls out that `generate_overview.py` uses `subprocess.run(batch_submit_command(...))` and then parses stdout lines such as `line.startswith(\"Submitted:\")` to recover the batch job ID. This is fragile compared with using native Python bindings, because CLI output format changes can break job submission or tracking without type-checked API guarantees.",
      "file": "scripts/generate_overview.py",
      "line": 0,
      "fix": "Replace CLI shell-out submission with the native `llmx` Python client/API and consume the returned batch/job object directly instead of parsing stdout text.",
      "confidence": 0.94,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 6
    },
    {
      "category": "architecture",
      "severity": "medium",
      "title": "Overview configuration contract is duplicated across multiple parsers",
      "description": "The logic for parsing `.claude/overview.conf` exists in three places: `shared.overview_config`, shell-based `grep/cut/xargs` in cron hooks, and an inline parser in the trigger hook. This creates a high risk of contract drift.",
      "file": "hooks/overview-staleness-cron.sh",
      "line": 0,
      "fix": "Centralize all configuration reading and marker path resolution into a single shared Python utility and invoke it from the shell hooks.",
      "confidence": 0.9,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 7
    },
    {
      "category": "logic",
      "severity": "medium",
      "title": "Plan-close budget metadata under-reports actual packet size",
      "description": "The `BudgetPolicy` limit (e.g., 40k chars) is attached to the packet but only describes the diff component. The total packet size includes file excerpts (up to 96k additional chars) and metadata, making the reported budget significantly smaller than the actual payload.",
      "file": "review/scripts/build_plan_close_context.py",
      "line": 0,
      "fix": "Update the manifest to report total packet size or clarify that the budget policy applies specifically to the diff component only.",
      "confidence": 0.9,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 8
    },
    {
      "category": "missing",
      "severity": "medium",
      "title": "Lack of verification for migration success criteria",
      "description": "The project claims 'implemented' status, but there are no shown tests enforcing live-vs-batch payload hash equivalence, marker semantics correctness, or plan-close golden output consistency.",
      "file": "scripts/test_generate_overview.py",
      "line": 0,
      "fix": "Implement regression tests that assert byte-for-byte equality between live and batch rendering paths and validate marker selection logic.",
      "confidence": 0.8,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 9
    },
    {
      "category": "logic",
      "severity": "medium",
      "title": "Overview renderer may drift prompt tag casing/formatting from the legacy shell prompt",
      "description": "The review highlights a potential compatibility problem: the old bash prompt used lowercase tags like `<instructions>` and `<codebase>`, while `generate_overview.py` now builds `PacketSection(\"Instructions\", ... tag=\"instructions\")` and depends on the `tagged` renderer to emit exactly the legacy shape. If the renderer uses title-cased section names, ignores `tag`, or adds formatting differences, the prompt signature changes even though the content source is similar.",
      "file": "scripts/generate_overview.py",
      "line": 0,
      "fix": "Add a prompt-shape regression test that snapshots the fully rendered overview prompt and verifies exact tag names/formatting, or hardcode renderer behavior to match the historical shell output exactly.",
      "confidence": 0.76,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 10
    },
    {
      "category": "style",
      "severity": "low",
      "title": "Project enumeration is hardcoded in multiple locations",
      "description": "Repo lists are hardcoded in both `scripts/generate_overview.py` and `hooks/overview-staleness-cron.sh`. Adding a new repository requires manual code edits in multiple files, which is not scalable.",
      "file": "scripts/generate_overview.py",
      "line": 0,
      "fix": "Replace hardcoded lists with an automatic discovery mechanism that searches for directories containing `.claude/overview.conf`.",
      "confidence": 0.9,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 11
    }
  ]
}
