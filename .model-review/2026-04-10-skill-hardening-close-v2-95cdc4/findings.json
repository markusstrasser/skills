{
  "findings": [
    {
      "category": "bug",
      "severity": "critical",
      "title": "`--extract` is documented but rejected by `model-review.py`",
      "description": "The review claims a contract mismatch between documentation and implementation: `review/SKILL.md`, `references/dispatch.md`, and `lenses/adversarial-review.md` instruct users to run `model-review.py` with `--extract`, but the script's `argparse` only exposes `--no-extract`. According to the reviewer, passing `--extract` currently causes an `unrecognized arguments` error and crashes the command.",
      "file": "review/scripts/model-review.py",
      "line": 0,
      "fix": "Replace the negative-only flag with `parser.add_argument(\"--extract\", action=argparse.BooleanOptionalAction, default=True)` so both `--extract` and `--no-extract` are accepted, matching the documented CLI.",
      "confidence": 0.98,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 1
    },
    {
      "category": "bug",
      "severity": "high",
      "title": "Path instability in selective manifest linting mode",
      "description": "The script accepts --manifest as raw Path and prints manifest_path.relative_to(ROOT). A relative path like brainstorm/skill.json is not under absolute ROOT, so relative_to() raises ValueError, crashing the linter.",
      "file": "scripts/lint_skill_manifests.py",
      "line": 0,
      "fix": "Normalize all manifest CLI inputs to repo-rooted absolute paths before validation/output by converting each to (ROOT / path).resolve() when relative.",
      "confidence": 1.0,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 2
    },
    {
      "category": "bug",
      "severity": "high",
      "title": "Validator crash on malformed uses.* entries",
      "description": "The validate_manifest function tests array items for membership (e.g., in PROFILES). Non-string unhashable items like dicts or lists in uses.dispatch_profiles, uses.packet_builders, or uses.artifact_schemas will raise TypeError instead of returning a validation issue.",
      "file": "shared/skill_manifest.py",
      "line": 0,
      "fix": "Validate that each array member is a non-empty string before performing membership checks.",
      "confidence": 1.0,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 3
    },
    {
      "category": "bug",
      "severity": "medium",
      "title": "Custom questions JSON is read without error handling",
      "description": "The review flags `question_overrides = json.loads(args.questions.read_text())` in `model-review.py` as fragile because it does not catch `JSONDecodeError` for malformed JSON or `FileNotFoundError` if the file disappears after an existence check. The reviewer notes this would surface a raw Python stack trace instead of a controlled CLI error.",
      "file": "review/scripts/model-review.py",
      "line": 0,
      "fix": "Wrap the read/parse step in `try/except` for `json.JSONDecodeError` and `FileNotFoundError`, print a descriptive error to `stderr`, and exit with status 1.",
      "confidence": 0.93,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 4
    },
    {
      "category": "logic",
      "severity": "medium",
      "title": "Review artifacts anchored to process CWD instead of project directory",
      "description": "Review output directories are built using Path('.model-review/...') relative to the process CWD rather than the project directory resolved from --project. This causes artifacts to be written to the wrong tree if the script is invoked from outside the target repository.",
      "file": "review/scripts/model-review.py",
      "line": 0,
      "fix": "Anchor review_dir creation to project_dir or add an explicit --output-dir argument.",
      "confidence": 0.9,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 5
    },
    {
      "category": "missing",
      "severity": "medium",
      "title": "Manifest validator ignores `requires_packet` and `requires_gpt` mode flags used in tests",
      "description": "The review says `scripts/test_skill_manifest.py` includes a mock `skill.json` with `\"requires_packet\": true` and `\"requires_gpt\": true` inside `modes`, but `shared/skill_manifest.py` does not validate or acknowledge those fields and only checks `intent_class` and `artifacts`. This is presented as a schema gap between tests and validator behavior.",
      "file": "shared/skill_manifest.py",
      "line": 0,
      "fix": "Extend `skill_manifest.py` to explicitly validate `requires_packet` and `requires_gpt` as booleans when present, and consider warning on unknown keys inside `modes`.",
      "confidence": 0.84,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 6
    },
    {
      "category": "missing",
      "severity": "medium",
      "title": "CLI tests omit the documented `--extract` path",
      "description": "The review identifies a test gap in `test_model_review.py`: it covers `--no-extract` and `--verify` but does not test the documented happy path that invokes the script with `--extract`. The reviewer argues this blind spot is why the CLI contract mismatch was able to slip through.",
      "file": "test_model_review.py",
      "line": 0,
      "fix": "Add CLI-level tests that invoke the parser/script with `--extract`, including combinations such as `--extract --verify`, and assert the command does not fail on argument parsing.",
      "confidence": 0.83,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 7
    },
    {
      "category": "missing",
      "severity": "medium",
      "title": "Brainstorm schema registry underspecifies documented artifact contract",
      "description": "Registry entries for brainstorm.matrix.v1 and brainstorm.coverage.v1 require fewer fields than documented (missing transfer_mechanism, caller_evidence, etc.), allowing malformed artifacts to pass contract checks.",
      "file": "shared/skill_manifest.py",
      "line": 0,
      "fix": "Expand ARTIFACT_SCHEMAS definitions to match the documented matrix/coverage requirements or update documentation to match the registry.",
      "confidence": 0.8,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 8
    },
    {
      "category": "style",
      "severity": "low",
      "title": "JSONL output escapes Unicode despite using UTF-8 file handles",
      "description": "The review points out that `observe/scripts/observe_artifacts.py` opens files with `encoding=\"utf-8\"` but serializes records with `json.dumps(..., ensure_ascii=True)`. This forces non-ASCII characters into `\\uXXXX` escapes, which the reviewer says defeats the purpose of UTF-8 output, bloats logs, and makes grepping or reading Unicode content harder.",
      "file": "observe/scripts/observe_artifacts.py",
      "line": 0,
      "fix": "Change serialization to `json.dumps(record, sort_keys=True, ensure_ascii=False)` when writing UTF-8 JSONL.",
      "confidence": 0.91,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 9
    },
    {
      "category": "logic",
      "severity": "low",
      "title": "Contradictory fallback policy in review documentation",
      "description": "Documentation contains internal contradictions, stating both that models should NEVER be downgraded on failure while also documenting automatic Gemini Pro to Flash fallback behavior.",
      "file": "review/lenses/adversarial-review.md",
      "line": 0,
      "fix": "Align the documentation with actual implemented policy: automated fallback is allowed as implemented by the dispatch layer.",
      "confidence": 0.9,
      "source_axis": "formal",
      "source_model": "gpt-5.4",
      "source_label": "GPT-5.4 (quantitative/formal)",
      "id": 10
    },
    {
      "category": "logic",
      "severity": "low",
      "title": "Prompt documentation embeds Bash subshells that may leak literal control logic to agents",
      "description": "The review warns that `review/references/prompts.md` contains prompt templates with Bash expressions such as `$([ -n \"$CONSTITUTION\" ] && echo ...)`. If those templates are loaded programmatically as raw prompt text instead of executed in Bash, the control logic would be passed literally into the LLM context rather than expanded.",
      "file": "review/references/prompts.md",
      "line": 0,
      "fix": "Rewrite the prompt templates as plain static text or isolate shell-only templating from agent-consumable prompt content so programmatic loaders do not ingest Bash expressions literally.",
      "confidence": 0.61,
      "source_axis": "arch",
      "source_model": "gemini-3.1-pro-preview",
      "source_label": "Gemini (architecture/patterns)",
      "id": 11
    }
  ]
}
