1. `research-ops/scripts/run-cycle.sh`: Path bug on line 69; `$SKILL_DIR` resolves to `research-ops/`, causing the script to look for `research-ops/scripts/llm-dispatch.py` which does not exist (the script is at the repo root `scripts/llm-dispatch.py`).
2. `research-ops/SKILL.md`: The "Shared dispatch" example (line 120) is missing the `--output` argument, which is marked as `required=True` in `scripts/llm-dispatch.py`.
3. `hooks/pretool-llmx-guard.sh`: The list of known valid flags (line 55) includes `--fallback`, but this flag is not supported by `scripts/llm-dispatch.py` or defined in the `DispatchProfile` or `DispatchOverrides` classes in `shared/llm_dispatch.py`.
4. `hooks/pretool-llmx-guard.sh`: The list of valid model names (lines 66-70) includes `gpt-5.2`, `gpt-5.3-chat-latest`, `o4-mini`, and `claude-sonnet-4-6`, none of which are defined in `shared/llm_dispatch.py` profiles or the `MODEL_TO_PROFILE` mapping.
5. `llmx-guide/SKILL.md`: References to `gpt-5.3-chat-latest` and `claude-sonnet-4-6` (lines 30, 150) are stale as they are not present in the new `shared/llm_dispatch.py` configuration.
6. `llmx-guide/SKILL.md`: Checklist item 2 mentions a 900s max timeout, but the `gpt_general` and `formal_review` profiles in `shared/llm_dispatch.py` are configured with a `600` default.
7. `hooks/generate-overview.sh`: `OVERVIEW_TYPES`, `OVERVIEW_PROFILE`, `OVERVIEW_MODEL`, `OVERVIEW_OUTPUT_DIR`, `OVERVIEW_PROMPT_DIR`, and `OVERVIEW_EXCLUDE` are redundantly initialized with defaults at lines 18-23 and then again at lines 82-87.
8. `hooks/pretool-llmx-guard.sh`: The "Gemini Pro without --stream" warning (line 114) is unreachable because `llmx chat` commands are blocked and the script exits on line 108.
9. `llmx-guide/SKILL.md`: The "Codex -o Gotcha" section and the "Model selection" table (lines 135, 230) still reference `codex exec` and raw `llmx chat` commands, which contradicts the new "Automation path" to use `llm-dispatch.py`.
10. `shared/llm_dispatch.py`: `MODEL_TO_PROFILE` (line 125) maps `gpt-5.4` to `gpt_general`, but `hooks/generate-overview.sh` uses a hardcoded mapping in `resolve_overview_profile` (line 114) for the same model.
11. `improve/SKILL.md`: The "Tier 2" instruction (line 276) suggests using `--profile cheap_tick`, but the detailed Python example earlier (line 188) uses `profile="deep_review"`.
12. `research-ops/scripts/run-cycle.sh`: The script captures only the first 80 lines of state (line 21), which might truncate the `STATE` variable used in the subsequent prompt assembly, potentially losing "NO STATE CHANGE" markers or critical context.
13. `hooks/generate-overview.sh`: The logic to extract `resolved_model` from JSON (line 246) uses bare `python3` rather than the `uv run` pattern used for the main dispatch, creating a potential inconsistency in dependency management.
14. `llmx-guide/SKILL.md`: The "Before You Call llmx" checklist still references `llmx "analyze this" -f context.md` (line 37), which uses the now-blocked CLI syntax instead of the new `llm-dispatch.py` syntax.
15. `brainstorm/references/llmx-dispatch.md`: Uses `deep_review` profile for parallel generation (line 26) but `fast_extract` for extraction (line 138), whereas `improve/SKILL.md` suggests `cheap_tick` for similar low-cost maintenance/triage tasks.