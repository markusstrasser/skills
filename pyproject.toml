[project]
name = "agent-skills-local"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0.3",
    "requests>=2.31",
]

[dependency-groups]
dev = [
    "pytest>=8.4",
    "ruff>=0.14",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
typeCheckingMode = "basic"
